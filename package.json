{
  "name": "webgpu-mechanical-wheel",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "vitest run",
    "test:browser": "playwright test",
    "analyze:distribution": "vitest run --config vitest.distribution.config.ts",
    "test:determinism": "playwright test --config playwright.determinism.config.ts",
    "generate:site-assets": "node scripts/generate-site-assets.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.4.1",
    "@webgpu/types": "^0.1.64",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4",
    "wabt": "^1.0.39"
  }
}
